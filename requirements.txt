# BUNKER 2.0 runtime dependencies — Python 3.10+ required
cryptography>=42.0.0
argon2-cffi>=23.1.0
inputimeout>=1.0.4
pyperclip>=1.8.2
psutil>=5.9.0
requests>=2.32.0
# Note: 'keyboard' is intentionally NOT listed — it was an unused import
# (removed from BUNKER.py) and requires root on Linux.
